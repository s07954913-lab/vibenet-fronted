import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DetailvibenetPage } from './detailvibenet.page';

describe('DetailvibenetPage', () => {
  let component: DetailvibenetPage;
  let fixture: ComponentFixture<DetailvibenetPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(DetailvibenetPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
